apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../rbac.yaml
  - ../server.yaml
  - ../network-policy.yaml

namespace: remolt-staging

patches:
  - path: server-patch.yaml
    target:
      kind: Deployment
      name: remolt-server
